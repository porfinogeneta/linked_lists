#include "stdio.h"
#include "stdlib.h"
#include "functions.h"

struct elem *mergeOrdered( struct elem *lista1, struct elem *lista2){
    struct elem *merged;
    struct elem *posB; // beginning of positives
    struct elem *posE; // end of positives
    struct elem *negB; // beginning
    struct elem *negE; // end
    while (lista1->next != NULL && lista2->next != NULL){

    }
}